extends layout

block content
  div(class='card border-danger shadow-sm mt-4')
    div(class='card-header bg-danger text-white')
      h4(class='mb-0') ⚠️ #{error.status || 500} — #{message}
    div(class='card-body')
      pre(class='text-danger small mb-0') #{error.stack}